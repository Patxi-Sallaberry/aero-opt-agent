# ─────────────────────────────────────────────────────────────
# Core First v1.0 — dépendances minimales
# Installation : python3 -m pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# Configuration & validation (Phase 0)
PyYAML==6.0.2            # lecture/écriture design_params.yaml & cfd_settings.yaml

# Agent / orchestrateur LLM (Phase 4)
anthropic==0.39.0        # client API Claude

# Tests (toutes phases)
pytest==8.3.3

# ─────────────────────────────────────────────────────────────
# CE QUI N'EST VOLONTAIREMENT PAS LÀ
#
# numpy : le post-traitement ne fait que des moyennes et des écarts-types
#   sur quelques milliers de points. La bibliothèque standard suffit, et
#   chaque dépendance en moins est une installation qui ne peut pas casser.
#
# python-dotenv : `pipeline.utils.load_env()` fait la même chose en vingt
#   lignes. Le driver tourne dans l'interpréteur embarqué de Fusion, où
#   l'on ne peut rien installer : moins il dépend de paquets externes,
#   mieux il se porte.
#
# Le driver Fusion n'utilise d'ailleurs AUCUNE de ces dépendances — il
# retombe sur un lecteur YAML minimal si PyYAML est absent.
# OpenFOAM est une dépendance système, pas une dépendance pip.
# ─────────────────────────────────────────────────────────────
